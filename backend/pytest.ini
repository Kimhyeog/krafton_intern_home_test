[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: 외부 의존성 없이 단독 실행 가능한 단위 테스트
    integration: DB/VertexAI를 mock으로 대체한 통합 테스트
    simulation: import 불가 모듈의 로직을 동일 설정으로 재현한 시뮬레이션 테스트
